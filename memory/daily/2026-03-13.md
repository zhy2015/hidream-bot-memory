
## 2026-03-13 10:38 (UTC+8) - Metal Anchor 运维与排障
- **Nginx 配置修复**：解决了手相分析前端的 Mixed Content 报错。将 `metal-anchor.site/api` 代理到本地的 8000 端口，并添加了 `client_max_body_size 50M;` 解决 Nginx 拦截大图片上传的问题。
- **后端服务恢复**：切换到 `main` 分支拉取最新代码，使用 `.venv` 虚拟环境通过 `uvicorn` 成功启动了后端服务（端口 8000）。
- **当前排障进度**：手相图片上传报错 `Unexpected token 'E', "Error occu"... is not valid JSON`。目前正在检查 `/api/analyze-hand` 接口和 `llm_client.py` 中的 LLM 调用链路。初步排查中发现 OPTIONS/HEAD 请求返回 405。
